import { Scale3D } from "lucide-react";

export default function QuantumForms () {
    return <Scale3D size={20} className="bg-gradient-to-r bg-clip-text text-transparent from-purple-600 to-orange-500" />
}